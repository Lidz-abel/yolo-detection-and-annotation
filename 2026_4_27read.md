# YOLO Project Handoff

日期：`2026-04-27`

本文件用于把当前 `YOLO` 项目的状态完整交接到另一台服务器，方便后续继续开发、训练、评估与写报告。

---

## 1. 顶层目录说明

当前项目主要有两个核心工程目录：

- `YOLO/yolov0`
  - checkpoint 6 主实验线
  - 已完成 backbone 演化、完整 loss、三框改进、Round 5/6 系列实验
- `YOLO/yolo_final`
  - 新的正式工程目录
  - 目标是继续向**双尺度三框正式检测器**推进

建议后续工作主线：

- 以 `yolo_final` 为后续正式开发目录
- `yolov0` 保留为 checkpoint 6 历史实验基线与报告依据

---

## 2. `yolov0` 当前结论

### 2.1 已完成的主阶段

`yolov0` 已经完成：

1. backbone 演化实验
   - `baseline_cnn`
   - `deep_cnn`
   - `residual_small`
   - `resnet18_like`
2. 完整 YOLO-style loss 接入
3. Stage A / B / C
   - `Stage A`: `deep_cnn + single-box full loss`
   - `Stage B`: `deep_residual + single-box full loss`
   - `Stage C`: `deep_residual + three-box full loss`
4. Round 4 / Round 5 / Round 6 系列改进

### 2.2 当前训练侧、推理侧保留结论

截至当前：

- **训练侧主基线**：
  - `Round 5C`
  - 含义：
    - three-box
    - decoupled head
    - dynamic assignment
    - quality-aware classification

- **推理侧排序参考**：
  - `Round 6A`
  - 含义：
    - 主要通过推理端 score 设计改善排序质量

- **不保留的尝试**
  - `6C`：
    - `dynamic_topk = 1` 过度收紧，退化
  - `6B`：
    - 训练损失更低，但检测指标未超过 `5C`

### 2.3 当前 `yolov0` 关键结论

当前单尺度三框主线已经说明：

- 单尺度三框 + 动态分配是有效的
- 继续在单尺度上做细碎 loss/matcher 微调，收益已开始变小
- 当前瓶颈更像是：
  - 单尺度 `10x10` 过粗
  - 密集目标 / 小目标表达受限

因此后续方向转向：

- **多尺度**
- 最终目标是更接近主流 YOLO 工程结构

---

## 3. `yolov0` 关键文档与报告位置

### 3.1 主要报告

- `YOLO/yolov0/report/checkpoint06_version01.pdf`
- `YOLO/yolov0/report/checkpoint06_version02.pdf`
- `YOLO/yolov0/report/checkpoint06_version03.pdf`

### 3.2 关键总结文档

- `YOLO/yolov0/logs/stagec.md`
- `YOLO/yolov0/logs/detection_evaluation_summary.md`
- `YOLO/yolov0/logs/stagec_round6_comparison.md`
- `YOLO/yolov0/logs/experiment_result_index.md`

### 3.3 指标和实验索引

后续如果要回查 `yolov0` 的所有实验，请优先看：

- `YOLO/yolov0/logs/experiment_result_index.md`

里面记录了：

- 实验名
- 配置文件
- run id
- result.txt
- metadata.json
- eval / coco_eval

---

## 4. `yolo_final` 当前状态

`yolo_final` 是后续主开发目录。

### 4.1 已完成的事情

已经完成：

1. 工程目录建立
2. 从 `yolov0` 迁移成熟代码链路
3. 双尺度三框正式计划文档
4. 双尺度三框正式实现接线
5. 轻量 neck 接入
6. 重新核验 smoke 通过

### 4.2 当前 `yolo_final` 已具备的能力

当前代码已经支持：

- 双尺度输出
  - `p4 = 20x20`
  - `p5 = 10x10`
- 每尺度三框
  - 每层输出 `255 = 3 * (80 + 5)`
- 每尺度独立 anchor
  - `anchors_p4`
  - `anchors_p5`
- 解耦检测头
- 轻量 neck
  - `fpn_pan_lite`
- 双尺度 loss 聚合
- 双尺度 prediction / internal eval / COCO eval / visualization 接线

### 4.3 当前 `yolo_final` 的关键配置

正式配置：

- `YOLO/yolo_final/configs/dual_scale_three_box_formal.toml`

当前 smoke 已验证：

- `feature_levels = ['p4', 'p5']`
- `grid_sizes = [20, 10]`
- `p4 output shape = (1, 20, 20, 255)`
- `p5 output shape = (1, 10, 10, 255)`
- `loss total / box / obj / cls` 都能正常算出

### 4.4 关键实现文件

#### 模型

- `YOLO/yolo_final/models/backbone.py`
- `YOLO/yolo_final/models/neck.py`
- `YOLO/yolo_final/models/head.py`
- `YOLO/yolo_final/models/detector.py`

#### 数据

- `YOLO/yolo_final/data/target_encoder.py`
- `YOLO/yolo_final/data/detection_dataset.py`

#### Loss

- `YOLO/yolo_final/losses/yolo_loss.py`

#### 工具

- `YOLO/yolo_final/tools/train.py`
- `YOLO/yolo_final/tools/evaluate.py`
- `YOLO/yolo_final/tools/evaluate_coco.py`
- `YOLO/yolo_final/tools/smoke_dual_scale_three_box.py`

#### 核心工具链

- `YOLO/yolo_final/utils/config.py`
- `YOLO/yolo_final/utils/prediction.py`
- `YOLO/yolo_final/utils/evaluation.py`
- `YOLO/yolo_final/utils/coco_eval.py`
- `YOLO/yolo_final/utils/visualization.py`

### 4.5 关键说明文档

- `YOLO/yolo_final/README.md`
- `YOLO/yolo_final/logs/implementation_status.md`
- `YOLO/yolo_final/logs/dual_scale_three_box_master_plan.md`
- `YOLO/yolo_final/logs/dual_scale_three_box_architecture.md`
- `YOLO/yolo_final/logs/dual_scale_three_box_implementation_notes.md`
- `YOLO/yolo_final/logs/experiment_protocol.md`
- `YOLO/yolo_final/logs/metric_dictionary.md`

---

## 5. `yolo_final` 当前未完成事项

当前还**没有完成**的是：

1. 双尺度三框正式 full-run
2. 双尺度三框 internal eval
3. 双尺度三框 COCO subset eval
4. 双尺度三框正式可视化归档
5. Stage D 的正式实验总结与对比表

也就是说：

> `yolo_final` 的代码链路已经打通，但正式实验结果还没开始积累。

---

## 6. 为什么 `yolo_final` 还没开始正式训练

不是代码问题，而是资源问题。

当时服务器 8 张 GPU 长时间被其他用户任务占满，具体占用用户为：

- `xiexh`

对应为 8 个推理进程，占满全部 GPU。

所以 `yolo_final` 目前停在：

- 代码实现完成
- smoke 通过
- 等待 GPU 空闲进入正式训练

---

## 7. 在新服务器上的建议启动顺序

后续在新服务器上，建议严格按下面顺序推进。

### 7.1 先做环境验证

建议先在新服务器上验证：

1. `conda` 环境可用
2. `yolov1` 环境依赖正常
3. `torch / torchvision / pycocotools` 正常
4. 数据集路径是否一致
5. `Unified/manifests` 是否可访问

### 7.2 先做代码 smoke

先运行：

- `YOLO/yolo_final/tools/smoke_dual_scale_three_box.py`

确认：

- 双尺度三框输出维度正常
- loss 可算
- 数据路径无误

### 7.3 再做第一轮正式 full-run

正式配置：

- `YOLO/yolo_final/configs/dual_scale_three_box_formal.toml`

第一轮 full-run 目标：

- 先拿到正式训练结果
- 不急着立刻改结构

### 7.4 full-run 结束后固定做的事

每一轮正式实验都必须：

1. 生成：
   - `config.toml`
   - `result.txt`
   - `metadata.json`
2. 跑：
   - internal eval
   - COCO subset eval
3. 生成：
   - GT vs Pred 可视化
4. 更新：
   - 实验索引
   - 检测评估总结
5. 写：
   - 阶段总结 `.md`
6. 做：
   - 专用 git commit

---

## 8. `yolo_final` 后续建议路线

推荐后续技术路线如下：

### 第一阶段

- 先做 `dual_scale_three_box_formal.toml` 第一轮正式训练
- 记录：
  - loss
  - internal `precision / recall / mAP@0.5`
  - COCO `AP50 / AR@100`
  - 可视化结果

### 第二阶段

根据第一轮结果判断：

- 如果双尺度三框已经明显优于 `yolov0` 单尺度三框主基线：
  - 继续保留多尺度主线
- 如果提升有限：
  - 再考虑更强的 neck 或更细的尺度扩展

### 第三阶段

如果双尺度三框成立，后续可继续考虑：

- 更轻量或更强的 neck
- 第三尺度
- 更正式的多尺度分配优化
- 后续增广策略

---

## 9. 当前最重要的工程原则

后续继续做时，保持以下原则：

1. 每轮正式实验都要落盘
2. 每轮正式实验都要有对应 `git commit`
3. 每轮都要保留配置和结果文件的对应关系
4. 每轮都要生成可视化，不能只看 loss
5. 报告里要能追溯到：
   - 配置文件
   - run id
   - result.txt
   - metadata.json
   - eval json
   - coco eval json

---

## 10. 当前最建议直接做的下一步

在新服务器上，最应该直接做的是：

1. 进入 `YOLO/yolo_final`
2. 先跑 `smoke_dual_scale_three_box.py`
3. 再用 `dual_scale_three_box_formal.toml` 启动第一轮 full-run
4. full-run 结束后补齐：
   - internal eval
   - COCO subset eval
   - visualization
   - summary
   - git commit

---

## 11. 一句话交接总结

当前项目状态可以概括为：

- `yolov0`：checkpoint 6 主实验线已基本完成，当前最强结论已明确
- `yolo_final`：双尺度三框正式工程已经实现并通过 smoke，但正式 full-run 尚未开始

后续的主任务已经非常明确：

> 在新服务器上，直接从 `yolo_final` 的双尺度三框第一轮正式实验开始。
